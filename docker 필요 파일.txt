📁 전송해야 할 폴더 (1개)
src (메인 소스 코드 폴더)

📄 전송해야 할 파일 (9개)
index.js (실행 엔트리포인트)
package.json (패키지 정보)
package-lock.json (패키지 잠금 정보)
Dockerfile (도커 빌드 명세서)
docker-compose.yml (도커 실행 환경 구성)
.dockerignore (도커 빌드 필터 설정)
.env (보안 설정값)
token.json (구글 Oauth 인증 토큰)
credentials.json (구글 API 접근 자격 증명)