# 📝 Troubleshooting & Session History

## 📌 핵심 운영 원칙 및 기준 폴더
- **기준 작업 폴더**: `d:\Antigravity\Workspace\Mail-Automator_gemma4`
- **원격 저장소**: `https://github.com/dicapriokim/Mail-Automation-localAI.git`
- **중요 지침**: Mail-Automator 프로젝트의 모든 작업, 검수, 커밋 및 배포의 기본 대상은 `Mail-Automator` 폴더가 아니라 **`Mail-Automator_gemma4`** 폴더입니다. 향후 에이전트는 혼동 없이 반드시 이 폴더를 기준으로 작업을 수행해야 합니다.

---

## 📅 세션 이력 (Session History)

### 2026-06-04 ~ 2026-06-05 세션 (Ollama 마이그레이션 완료)
- **수행 작업**:
  1. **LLM 백엔드 전환**: 기존 외부 Google Gemini Cloud API 의존성을 100% 제거하고, 로컬 Proxmox LXC 환경의 Ollama (`qwen2.5:3b`) 내장 OpenAI 호환 API(`http://[IP]:11434/v1/chat/completions`)로 마이그레이션을 완료했습니다.
  2. **동적 IP 디스커버리 탑재**:
     - `.env`에 등록된 고정 IP 우선 탐색.
     - 실패 시 mDNS (`superllm.local`) 탐색.
     - 실패 시 C클래스 사설 서브넷 스캔(192.168.0.x)을 통한 11434 포트 탐색.
     - 최종 실패 시 `127.0.0.1` 루프백 폴백.
     - 결과 캐싱 및 연결 오류 발생 시 캐시 리셋 로직 반영.
  3. **문서 및 설정 정비**:
     - `README.md`에 Proxmox LXC 올인원 구축 가이드 링크 및 단일 vs 분리 운영 장단점 구성 팁 추가.
     - `.gitignore`에 보안 강화를 위한 임시 백업(`*_bak`, `*.bak`, `*output.txt`) 패턴 등록.
     - `.env.example`에 `LOCAL_AI_IP` 추가 및 사용하지 않는 Gemini API Key 예시 항목 제거.
     - `.agent` 프로젝트 지식 및 스킬북 동기화 완료.
- **성공 단계**:
  - `Mail-Automator_gemma4` 폴더에 최종 마이그레이션 코드를 성공적으로 이식 및 검증 완료.
  - `Mail-Automation-localAI.git` 원격 저장소의 `main` 브랜치로 푸시 완료.
- **특이 사항**:
  - 로컬에 유사한 이름의 `Mail-Automator` 폴더가 있어 이전 에이전트들의 경로 판단에 혼선이 있었으나, 현재 기준 폴더를 `Mail-Automator_gemma4`로 명확히 단일화하여 문서 및 환경을 모두 동기화 완료했습니다.

### 2026-06-06 세션 (카카오 잔재 청산 및 텔레그램 가이드 보완)
- **수행 작업**:
  1. **카카오 잔재 완벽 청산**: README.md, .gitignore, .env.example에서 불필요한 카카오톡 알림 관련 기술 스택 설명, Python 의존성(pip install) 가이드, 및 kakao_token.json 설정을 완전히 제거했습니다.
  2. **구글 자격증명 가이드 보완**: README.md에 credentials.json 발급 프로세스(Gmail, Docs, Drive API 활성화 및 데스크톱 앱 클라이언트 ID 다운로드)와 	oken.json 최초 자동 생성 프로세스(Authorization Code 입력)의 상세 가이드를 보완 작성했습니다.
  3. **구글 독스 출력 서식 동기화**: src/summarize.js 내 정리 통계 블록의 글머리 기호 -를 구글 독스 포맷 규정에 맞게 제거(순수 텍스트 줄바꿈 형태로 정규화)하여 스타일 일관성을 확보했습니다.
- **성공 단계**:
  - Mail-Automator_gemma4 기준 폴더의 모든 변경 사항을 원격 저장소(origin/main)에 푸시 완료.
- **특이 사항**:
  - src/cleanup.js의 rom:kakao 조건은 스팸 메일 오탐지 방지 목적의 정상 필터 로직이므로 검수 결과 의도적으로 유지(수정 제외)했습니다.
- **설치 디렉토리명 일관성 수정 (2026-06-06)**: README.md 내 로컬 PC 설치 가이드와 LXC 패키지 설치 가이드 간 클론 대상 폴더명이 불일치하던 부분을 Mail-Automator로 모두 통일하고 배포(Push)를 완료했습니다.
- **설치 동선 단일화 및 가이드 통합 (2026-06-06)**: README.md에서 로컬 설치(1~4단계)와 LXC 환경 설치(5단계)로 인해 이중으로 기재되어 있던 Git 클론, 패키지 설치 등의 중복을 제거하고, 1~4단계의 단일화된 통합 동선으로 전체 문서를 통합 정비하여 푸시했습니다.
- **구글 독스 문서 ID 가이드 추가 (2026-06-06)**: README.md에 GOOGLE_DOC_ID 환경 변수 설정 시 참고할 수 있도록 새 구글 문서의 최초 생성 방법과 브라우저 주소창(URL)을 통한 ID 확인/추출 가이드를 상세히 추가하고 배포(Push)를 마쳤습니다.
- **Ollama 요약 병목 해결 및 타임아웃 최적화 (2026-06-06)**: 10건의 이메일 본문을 한 번에 전송하여 로컬 LLM(qwen2.5:3b)의 연산 오버헤드로 인한 120초 연속 타임아웃(총 18분 지연)이 나던 현상을 해소했습니다. 메일당 최대 텍스트 길이를 1200자로 최적화하고, 배치 크기(CHUNK_SIZE = 3) 단위를 새로 도입하여 3건씩 분할 연산하도록 바꿨습니다. 또한 무의미한 루프 대기를 방지하기 위해 타임아웃을 45초로 낮추고 재시도(MAX_RETRIES) 횟수를 1회로 과감히 제한했습니다.
- **Ollama 순차 처리 테스트 최종 성공 (2026-06-06)**: CHUNK_SIZE = 1, MAX_RETRIES = 0, Timeout = 60s 및 IP 캐시 영구 보존 최적화를 적용한 최종 테스트를 수행했습니다. 이전 18분 이상 지연 및 전체 폭사 상태에서 **12분 41초로 소요 시간이 대폭 단축**되었으며, 일시적 타임아웃이 나더라도 IP를 잃지 않고 끝까지 완주하여 요약 성공 메일을 수집하고 구글 독스 기록 및 텔레그램 발송을 최종 완료했습니다.
- **CPU 최적화 모델 전환 및 가변 설정 도입 (2026-06-06)**: 사용자의 LXC 컨테이너 설정에 기재된 장치(/dev/dri/card0, renderD128)가 CUDA를 미지원하는 인텔/AMD 내장 그래픽(iGPU) 장치임을 규명했습니다. Ollama가 iGPU 가속을 잡지 못해 CPU로만 구동되면서 12분 이상 장시간이 걸리던 추론 속도 문제를 해소하고자, 기본 탑재 모델을 연산 효율이 극도로 뛰어난 llama3.2:1b로 즉시 교체하였습니다. 또한 향후 사용자가 GPU 가속 장치를 탑재하거나 임의의 모델로 원활히 커스텀할 수 있도록 .env 환경 변수 OLLAMA_MODEL을 통해 동적으로 요약 모델을 불러오도록 코드를 유연하게 개편하고 최종 푸시를 마쳤습니다.
- **GPU 가속 확인 및 배치 크기 복원 (2026-06-06)**: 사용자 측에서 Ollama GPU 가속(OpenVINO 또는 Intel/AMD 드라이버 매핑)이 성공적으로 활성화되어, 1건당 추론 시간이 2~5초 수준으로 단축된 것을 실측했습니다. 이에 따라 순차 호출(CHUNK_SIZE = 1)로 인해 불필요하게 9분 이상 소요되던 API 왕복 네트워크 지연 오버헤드를 극복하고자, 배치 크기를 다시 CHUNK_SIZE = 3으로 복원하여 API 다중 호출 병목을 정리하고 최종 배포(Push)를 마쳤습니다.

### 2026-06-13 세션 (LLM 환각 방어 및 메일 수집 병렬화)
- **수행 작업**:
  1. **네트워크 I/O 병렬화**: Gmail API와 Naver IMAP 수집 구간을 직렬에서 병렬(Promise.all)로 개편하여 수집 시간을 절반으로 단축했습니다.
  2. **JSON 환각(Hallucination) 방어**: N150의 CPU 연산 한계 및 qwen2.5:3b 모델의 배열 포맷 붕괴 현상을 방지하기 위해 CHUNK_SIZE를 1로 낮추고, 1건 단위 처리로 전환했습니다.
  3. **정규식 추출 및 자동 교정 강화**: 모델이 배열 기호([])를 누락한 채 객체({})만 반환하더라도 강제로 배열로 래핑하고, 포맷 오류 시 최대 1회 재시도(MAX_RETRIES=1)하도록 에러 핸들링을 고도화했습니다.
  4. **Graceful Fallback 적용**: 응답이 완전한 쓰레기값(수신: 0)인 경우에도 전체 배치를 중단시키지 않고, 해당 메일만 [AI 요약 지연]으로 안전하게 건너뛰도록 격리 로직을 완벽 검증했습니다.
- **성공 단계**:
  - 테스트 구동 결과 총 20건 중 17건 요약 성공, 3건 안전 격리로 정상 완주 확인.
- **특이 사항**:
  - N150 환경(100% CPU 구동)에서 qwen2.5:1.5b 다운그레이드 없이 3b 모델의 성능을 최대한 끌어내며 오류를 억제하는 최적의 합의점 도달.

### 2026-06-13 세션 (LLM System Prompt 완전 격리 및 CPU 극한 최적화)
- **수행 작업**:
  1. **HTML 노이즈 정규화**: 네이버 광고 메일의 `<style>`, `<script>` 블록을 완전 도려내어 본문 추출 품질을 극대화, 1200자 텍스트 제한에 걸려 실제 내용이 LLM에 전달되지 못하고 쓰레기값이 들어가던 현상 해결.
  2. **System Prompt 완전 격리**: `qwen2.5:3b` 모델이 긴 이메일 텍스트에 압도되어 한국어 강제 및 생략 방지 지시를 무시하던 현상을 해결하기 위해, `role: 'system'` 메시지로 절대 규칙을 분리하여 강제력을 대폭 상향시켰습니다.
  3. **요약 텍스트 줄바꿈 치환**: LLM이 임의로 출력하는 줄바꿈(`\n`)으로 인해 구글 독스 렌더링 인덱스가 붕괴(`➔ 후속 조치:`가 붕 뜨는 현상)하는 것을 막기 위해 모든 줄바꿈을 공백으로 강제 치환했습니다.
  4. **Ollama CPU(N150) 옵션 튜닝**: API 옵션으로 `num_ctx: 2048` 및 `num_thread: 4`를 강제 부여하여 불필요한 메모리 스와핑을 차단하고 4코어 연산 효율을 극대화했습니다. (타임아웃 180s로 연장)
- **성공 단계**:
  - 총 20건 중 19건 요약 시도하여 **19건 100% 요약 성공(0 에러, 0 지연, 0 환각)**, 총 소요 시간 10분 29초로 완벽한 안정성 달성.

### 2026-06-13 세션 (EXAONE 테스트 실패 및 극한 최적화)
- **수행 작업**:
  1. **모델 테스트 및 롤백**: EXAONE 모델 도입을 테스트했으나 호환성/성능 문제로 실패하여 기존 검증된 `Qwen2.5` 모델로 유지하기로 결정했습니다.
  2. **30일치 광고 싹쓸이 기능**: `cleanup.js`의 범용 광고 메일 삭제 조건을 1일에서 30일(`newer_than:30d`)로 대폭 확장하여 편지함 정리 능력을 강화했습니다.
  3. **연산 시간 최소화를 위한 극한 축소**: N150 환경의 속도 한계를 극복하기 위해 LLM에 전달하는 텍스트 길이를 1200자에서 600자로 반토막 내고, `num_ctx` 파라미터도 2048에서 1024로 극한 축소했습니다.
  4. **텔레그램 로깅 추가**: 알림 및 콘솔 로그에 실제 요약에 사용된 LLM 모델명(`usedModel`)이 출력되도록 `index.js`를 보완했습니다.
- **성공 단계**:
  - `README.md` 및 이력 문서에 변경사항 동기화 완료.
